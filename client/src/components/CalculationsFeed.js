import React from "react";

const CalculationsFeed = ({ calculations }) => {
  return (
    <div className="container">
      <div className="content">hello</div>
      {/* map calculations max limit 10 desc most rec to least */}
    </div>
  );
};

export default CalculationsFeed;
